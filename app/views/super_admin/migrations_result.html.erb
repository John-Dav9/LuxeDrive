<div class="card">
  <div class="card-body">
    <h1 class="mb-3">Résultat des migrations</h1>
    <p class="text-muted">Sortie de la commande <code>bin/rails db:migrate</code> :</p>
    <pre style="white-space: pre-wrap; background: #0f172a; color: #e2e8f0; padding: 1rem; border-radius: 12px;"><%= @migration_output.presence || "Aucune sortie." %></pre>
    <div class="mt-3">
      <%= link_to "Retour", super_admin_root_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>
