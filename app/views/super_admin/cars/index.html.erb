<div class="container-fluid">
  <div class="admin-page-header">
    <p class="header-eyebrow">Super Admin</p>
    <h1 class="header-title">
      <span class="header-icon">🚗</span>
      Gestion des voitures
    </h1>
    <p class="header-subtitle">Ajouter des photos et gerer les vehicules existants.</p>
  </div>

  <div class="card border-0 shadow-sm admin-table-card">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0 admin-table">
          <thead class="table-light">
            <tr>
              <th>ID</th>
              <th>Véhicule</th>
              <th>Propriétaire</th>
              <th>Catégorie</th>
              <th>Prix</th>
              <th>Photos</th>
              <th>Statut</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @cars.each do |car| %>
              <tr>
                <td><code><%= car.id %></code></td>
                <td>
                  <strong><%= car.brand %> <%= car.model %></strong>
                  <div class="text-muted small"><%= car.address %></div>
                </td>
                <td><%= car.user&.full_name || "—" %></td>
                <td><span class="badge bg-secondary"><%= car.category %></span></td>
                <td><%= number_with_precision(car.price, precision: 0) %> €/jour</td>
                <td><span class="badge bg-info"><%= car.photos.count %></span></td>
                <td>
                  <% if car.status %>
                    <span class="badge bg-success">Disponible</span>
                  <% else %>
                    <span class="badge bg-danger">Indisponible</span>
                  <% end %>
                </td>
                <td>
                  <div class="btn-group btn-group-sm">
                    <%= link_to "Ajouter photos", edit_super_admin_car_path(car), class: "btn btn-outline-primary" %>
                    <%= link_to "Supprimer",
                        super_admin_car_path(car),
                        data: { turbo_method: :delete, turbo_confirm: "Supprimer ce véhicule ?" },
                        class: "btn btn-outline-danger" %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <% if @cars.any? %>
      <div class="card-footer bg-white">
        <div class="d-flex justify-content-center">
          <%= paginate @cars %>
        </div>
      </div>
    <% end %>
  </div>

  <% if @cars.empty? %>
    <div class="alert alert-info mt-4">
      <i class="fas fa-info-circle me-2"></i>
      Aucune voiture trouvée.
    </div>
  <% end %>
</div>
