<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card border-0 shadow-sm">
        <div class="card-body">
          <h1 class="h3 mb-3">📸 Ajouter des photos</h1>
          <p class="text-muted mb-4">
            <strong><%= @car.brand %> <%= @car.model %></strong> — <%= @car.address %>
          </p>

          <%= simple_form_for([:super_admin, @car], html: { multipart: true }) do |f| %>
            <div class="mb-3">
              <%= f.input :photos,
                          as: :file,
                          label: "Sélectionner une ou plusieurs images",
                          input_html: { multiple: true, accept: "image/*", class: "form-control" } %>
            </div>

            <div class="form-check form-switch mb-4">
              <%= f.input :status, as: :boolean, label: "Disponible à la location" %>
            </div>

            <% if @car.photos.attached? %>
              <hr>
              <h2 class="h5">Photos existantes</h2>
              <p class="text-muted">Glisse-dépose pour réordonner.</p>
              <div data-controller="photo-sort">
                <%= f.input :photo_order, as: :hidden, input_html: { id: "photo-order-input", data: { photo_sort_target: "input" } } %>
                <div class="row g-2" data-photo-sort-target="container">
                  <% @car.ordered_photos.each do |photo| %>
                    <div class="col-6 col-md-4 photo-sort-item" data-photo-sort-target="item" data-photo-id="<%= photo.id %>" draggable="true">
                      <div class="position-relative">
                        <span class="badge bg-dark position-absolute top-0 start-0 m-2">⇅</span>
                        <%= image_tag photo, class: "img-fluid rounded border w-100" %>
                        <%= link_to "Supprimer",
                            super_admin_car_photo_path(@car, photo),
                            data: { turbo_method: :delete, turbo_confirm: "Supprimer cette photo ?" },
                            class: "btn btn-sm btn-danger position-absolute bottom-0 end-0 m-2" %>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            <% end %>

            <div class="d-flex gap-2">
              <%= f.submit "Mettre à jour", class: "btn btn-primary" %>
              <%= link_to "Retour", super_admin_cars_path, class: "btn btn-outline-secondary" %>
            </div>
          <% end %>

        </div>
      </div>
    </div>
  </div>
</div>
