<div class="container-fluid">
  <div class="owner-page-header">
    <p class="header-eyebrow">Proprietaire</p>
    <h1 class="header-title">
      <span class="header-icon">🚗</span>
      Gestion de mes voitures
    </h1>
    <p class="header-subtitle">Ajouter des photos, gerer vos vehicules et suivre leur disponibilite.</p>
  </div>

  <div class="card border-0 shadow-sm owner-table-card">
    <div class="card-body p-0">
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0 owner-table">
          <thead class="table-light">
            <tr>
              <th>ID</th>
              <th>Véhicule</th>
              <th>Catégorie</th>
              <th>Prix</th>
              <th>Photos</th>
              <th>Statut</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @cars.each do |car| %>
              <tr>
                <td><code><%= car.id %></code></td>
                <td>
                  <strong><%= car.brand %> <%= car.model %></strong>
                  <div class="text-muted small"><%= car.address %></div>
                </td>
                <td><span class="badge bg-secondary"><%= car.category %></span></td>
                <td><%= number_with_precision(car.price, precision: 0) %> €/jour</td>
                <td><span class="badge bg-info"><%= car.photos.count %></span></td>
                <td>
                  <% if car.status %>
                    <span class="badge bg-success">Disponible</span>
                  <% else %>
                    <span class="badge bg-danger">Indisponible</span>
                  <% end %>
                </td>
                <td>
                  <div class="btn-group btn-group-sm">
                    <%= link_to "Ajouter photos", edit_owner_car_path(car), class: "btn btn-outline-primary" %>
                    <%= link_to "Supprimer",
                        owner_car_path(car),
                        data: { turbo_method: :delete, turbo_confirm: "Supprimer ce véhicule ?" },
                        class: "btn btn-outline-danger" %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <% if @cars.any? %>
      <div class="card-footer bg-white">
        <div class="d-flex justify-content-center">
          <%= paginate @cars %>
        </div>
      </div>
    <% end %>
  </div>

  <% if @cars.empty? %>
    <div class="alert alert-info mt-4">
      <i class="fas fa-info-circle me-2"></i>
      Vous n'avez pas encore de voitures.
    </div>
  <% end %>
</div>
