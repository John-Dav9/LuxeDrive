<h1>Liste des voitures disponibles</h1>

<%= link_to "Ajouter une voiture", new_car_path, class: "btn btn-primary" %>

<ul>
  <% @cars.each do |car| %>
    <li>
      <h2><%= link_to "#{car.brand} #{car.model}", car_path(car) %></h2>
      <%= car.photos.attached? ? image_tag(car.photos.first, style: "width: 300px; height: auto;") : "Aucune photo disponible" %>
      <p>Prix : <%= car.price %> €/jour</p>
      <p>Localisation : <%= car.adress %></p>
    </li>
  <% end %>
</ul>